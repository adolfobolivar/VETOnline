/** UC-002 isn't implemented yet — honest placeholder, not stubbed fake data. */
export function VeterinariansPlaceholderPage() {
  return (
    <div className="screen-body">
      <div className="screen-head" style={{ padding: 0, marginBottom: "1.5rem" }}>
        <span className="eyebrow">UC-002 &middot; Veterinarians</span>
        <h2>Our veterinarians</h2>
      </div>
      <p style={{ color: "var(--text-muted)" }}>
        This screen isn't built yet — only Register New Owner (UC-003) and Add Pet to Owner
        (UC-007) are implemented so far.
      </p>
    </div>
  );
}
